services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - legal-ai-cache:/app/.cache
  redis:
    image: redis:7-alpine
    profiles: ["redis"]

volumes:
  legal-ai-cache:

